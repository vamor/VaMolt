{
  "name": "web-ui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "echo \"(placeholder) lint ok\"",
    "test": "node src/dummy.test.js",
    "build": "echo \"(placeholder) build ok\""
  }
}