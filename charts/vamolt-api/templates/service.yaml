apiVersion: v1
kind: Service
metadata:
  name: {{ include "vamolt-api.fullname" . }}
  labels:
    {{- include "vamolt-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "vamolt-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP